# 开发环境的配置

# 以 MAIN_VITE_ 为前缀的变量暴露给主进程，PRELOAD_VITE_ 用于预加载脚本，RENDERER_VITE_ 则用于渲染器，VITE_ 则所有进程共用

# MAIN_VITE_KEY=123      # 仅主进程可用
# PRELOAD_VITE_KEY=123   # 仅预加载脚本可用
# RENDERER_VITE_KEY=123  # 仅渲染器可用
# VITE_KEY=123           # 所有进程共用

VITE_API_URL=http://localhost:3000

